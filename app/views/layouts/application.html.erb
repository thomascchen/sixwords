<!DOCTYPE html>
<html>
<head>
<link href='http://fonts.googleapis.com/css?family=Libre+Baskerville' rel='stylesheet' type='text/css'>
  <title>Six Word Memoirs</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <nav class="top-bar">
    <ul class="title-area">
      <li class="name">
        <h1 class="nav-heading"><a href="/">Six Word Memoirs</a></h1>
      </li>
    </ul>

    <section class="top-bar-section">
      <ul class="right memoir-text">
        <li>
          <%= form_tag("/", method: "get") do %>
            <%= text_field_tag(:query, nil, required: true, value: "Search", class: "radius") %>
          <% end %>
        </li>
        <li><%= link_to "Random", memoir_path(Memoir.all.sample) %></li>
        <li><%= link_to "Neighborhoods", neighborhoods_path %></li>
        <li><%= link_to "Submit", new_memoir_path %></li>
      </ul>
    </section>
  </nav>

  <%- flash.each do |key, value| -%>
    <div class="flash flash-<%= key %>">
      <%= value %>
    </div>
  <%- end -%>

  <%= yield %>

  <%= javascript_include_tag 'application' %>
  <%= yield :extra_footer %>
</body>
</html>
